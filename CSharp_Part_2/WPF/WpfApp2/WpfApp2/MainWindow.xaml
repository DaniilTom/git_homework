<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="58*"/>
            <ColumnDefinition Width="41*"/>
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding colD}" AutoGenerateColumns="False" Name="dg"
                  ScrollViewer.CanContentScroll="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Department" IsReadOnly="True"
                                    Binding="{Binding FullName}" 
                                    CanUserSort="True"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <Button Height="64" VerticalAlignment="Top" Content="Редактировать" Click="Button_Click" Margin="12,0,10,0" Grid.Column="1"/>
        <TextBlock Text="Выберите департамент, список работников которого хотите посмотреть." Grid.Column="1" Margin="10,100" TextWrapping="Wrap"/>
    </Grid>
</Window>
